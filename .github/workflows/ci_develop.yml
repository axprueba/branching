name: CI Develop

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]
  workflow_dispatch:
    inputs:
      branch_name:
        description: "Branch to deploy from"
        required: true
        default: "develop"
        type: string

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_USERNAME }}
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump job context
        env:
          JOB_CONTEXT: ${{ toJson(job) }}
        run: echo "$JOB_CONTEXT"

      - name: Check out repository
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.branch_name || 'develop' }}

      - name: Docker login
        run: echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin ${{ env.DOCKER_REGISTRY }}

      - name: Build Docker image
        run: |
          echo "Building Docker image $DOCKER_REGISTRY${{ github.repository}}:dev-${{ github.sha }}"
          docker build . -t $DOCKER_REGISTRY${{ github.repository}}:dev-${{ github.sha }}
        
      - name: Push Docker image
        run: docker push $DOCKER_REGISTRY${{ github.repository}}:dev-${{ github.sha }}
        
      - name: Deploy to Dev
        run: |
          echo "Desplegando imagen $DOCKER_REGISTRY${{ github.repository}}:dev-${{ github.sha }} a entorno de desarrollo"
        
